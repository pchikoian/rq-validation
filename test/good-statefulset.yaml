# expect: ALLOWED
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: good-statefulset
  namespace: test-enforced
spec:
  serviceName: good-sts
  replicas: 1
  selector:
    matchLabels:
      app: good-sts
  template:
    metadata:
      labels:
        app: good-sts
    spec:
      containers:
        - name: app
          image: nginx:alpine
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
