# expect: DENIED — missing limits
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bad-deployment
  namespace: test-enforced
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bad
  template:
    metadata:
      labels:
        app: bad
    spec:
      containers:
        - name: app
          image: nginx:alpine
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            # limits not set
