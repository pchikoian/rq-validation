apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-quota
  namespace: test-enforced
spec:
  hard:
    requests.cpu: "4"
    requests.memory: "8Gi"
    limits.cpu: "8"
    limits.memory: "16Gi"
    # Add extended resources here to allow them, e.g.:
    # requests.nvidia.com/gpu: "4"
